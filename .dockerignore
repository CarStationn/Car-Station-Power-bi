# Contexto da imagem web: só os arquivos do frontend entram.
backend
.git
.github
.vscode
node_modules
*.md
Dockerfile*
docker-compose*.yml
# nginx.conf NÃO entra aqui: o Dockerfile.web precisa copiá-lo para a
# configuração do Nginx (e depois o apaga da pasta pública).
.env
.env.example
